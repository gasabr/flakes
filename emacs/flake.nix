{  inputs.emacs.nixpkgs ? null,  outputs = {    out ? let      pkgs = if inputs.emacs.nixpkgs != null then inputs.emacs.nixpkgs else import <nixpkgs> {};    in      with pkgs;        {          defaultPackage = pkgs.emacs;          devShell = pkgs.mkShell {            buildInputs = [              pkgs.emacs              pkgs.git              # Add any other dependencies needed for Emacs development            ];          };        };  }}